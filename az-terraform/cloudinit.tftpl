#cloud-config
system_info:
  default_user:
    shell: /usr/bin/zsh

runcmd:
  - dnf update -y
  - dnf install -y git python3-pip curl wget ca-certificates vim htop zsh jq gnupg podman
  - curl -fsSL https://tailscale.com/install.sh | sh
  # - ['sh', '-c', "echo 'net.ipv4.ip_forward = 1' | sudo tee -a /etc/sysctl.d/99-tailscale.conf && echo 'net.ipv6.conf.all.forwarding = 1' | sudo tee -a /etc/sysctl.d/99-tailscale.conf && sudo sysctl -p /etc/sysctl.d/99-tailscale.conf" ]
  - tailscale up --ssh --accept-routes --authkey=${tailscale_auth_key}
  - tailscale set --ssh --advertise-exit-node --relay-server-port=45129